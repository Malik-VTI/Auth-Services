apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 7070
      targetPort: 7070
      nodePort: 30001
  type: ClusterIP
